{
  "name": "@zua/grpc",
  "version": "1.4.0",
  "description": "Node gRPC interface for Zua",
  "main": "dist/index.js",
  "author": "ASPECTRON Inc",
  "license": "ISC",
  "scripts": {
    "watch": "tsc --watch",
    "prepublishOnly": "rm -rf ./dist && rm -f package-lock.json && npm install --also=dev && tsc"
  },
  "dependencies": {
    "@grpc/grpc-js": "*",
    "@grpc/proto-loader": "*",
    "@aspectron/flow-async": "^0.0.10"
  },
  "devDependencies": {
    "@types/node": "^14.14.9",
    "ts-node": "^9.0.0",
    "typescript": "*"
  },
  "files": [
    "dist",
    "lib",
    "types",
    "proto",
    "README.md",
    "tsconfig.json",
    "index.ts"
  ]
}
